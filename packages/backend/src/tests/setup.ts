import { beforeAll, afterAll, beforeEach, afterEach } from '@jest/globals'

beforeAll(async () => {
  // 全局测试设置
})

afterAll(async () => {
  // 全局测试清理
})

beforeEach(async () => {
  // 每个测试前的设置
})

afterEach(async () => {
  // 每个测试后的清理
})
